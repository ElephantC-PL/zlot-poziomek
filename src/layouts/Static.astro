---
import PrivacyConsentModal from '../components/PrivacyConsentModal.astro';
import BaseLayout from './Base.astro';

interface Props {
    title?: string;
    description?: string;  
    heroImage?: {
        height: number;
        src: string;
        width: number;
        format: "avif" | "png" | "webp" | "jpeg" | "jpg" | "svg" | "tiff" | "gif";
    }	
}

---
<BaseLayout>
	<slot />   
    <PrivacyConsentModal/> 
</BaseLayout>
