---
import type { Lang } from "../../i18n/config";
import { useTranslations } from "../../i18n/utils";
import BasicSection from "../../layouts/BasicSection.astro";
import MapIframe from "../MapIframe.astro";

const { data, isLast, isEven } = Astro.props;
const lang = Astro.currentLocale as Lang;
const t = useTranslations(lang);
---


<BasicSection sectionType={data.type} isLast={isLast}>
    <h2 class="text-center text-primary">{t('section.' + data.type)}</h2>
    <div>        
        <slot />
    </div>     
    <!-- <div class="collapse collapse-arrow">
        <input type="checkbox" name="registryForm" />
        <button class="collapse-title btn btn-wide btn-primary mx-auto">{t('buttons.registryForm')}</button>
        <div class="collapse-content px-0">
            <div class="mx-auto w-full max-w-[640px] rounded-lg overflow-hidden mt-4">
              <iframe class="bg-white w-full max-w-[640px] border-radius" src="https://docs.google.com/forms/d/e/1FAIpQLSfjszgyUIcM7t0A9L3EH7hc3OZoue2Npl6pRtiyWyw5BPEv3A/viewform?embedded=true" height="2800" frameborder="0" marginheight="0" marginwidth="0">Ładuję…</iframe>
            </div>
        </div>               
    </div>     -->
</BasicSection>